service:
  name: hexo

provider:
  name: aliyun
  runtime: nodejs10

# deployType:
#   type: static
#   config:
#     rewrite:
#       /(.*): /index.html

package:
  include:
    - public
  artifact: code.zip

custom:
  customDomain:
    domainName: 'hexo.qingrizhi.com'
