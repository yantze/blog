doctype
html(lang=config.language)
    head
        include head
    body
        .wrap
            header
                a.logo-link(href=url_for())
                    block site_title
                        = config.title
                include nav
            main.container
                block container
            footer
                block pagination
                block copyright
        include scripts
